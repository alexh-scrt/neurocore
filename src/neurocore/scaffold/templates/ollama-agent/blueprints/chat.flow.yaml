name: "{{ project_name }}-chat"
version: "1.0"
description: "Single-step local LLM chat."
components:
  - name: chat
    type: chat
    config:
      system: "You are a concise, helpful assistant."
flow:
  type: sequential
  steps:
    - component: chat
